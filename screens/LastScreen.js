import {Text,StyleSheet} from 'react-native';

function LastScreen(){ 
    
    <Text style={styles.container}>Last Screen</Text>
}

export default LastScreen;


const styles = StyleSheet.create({
    container: {
      width: "100%",
      height: "100%",
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
    }});